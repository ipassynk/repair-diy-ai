{
  "name": "backend",
  "version": "1.0.0",
  "description": "Repair DIY AI Backend - FastAPI, Pydantic, OpenAI, pandas",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "setup": "pip install -r requirements.txt",
    "setup-dev": "pip install -r requirements.txt && pip install -r requirements-dev.txt"
  },
  "engines": {
    "python": ">=3.8"
  }
}
